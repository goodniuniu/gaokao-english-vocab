{
  "name": "gaokao-vocab-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for gaokao-english-vocab cloud sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
