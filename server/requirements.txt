# 高考英语单词练习 - FastAPI 后端依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.110.0

# ASGI 服务器（生产用 uvicorn；systemd 拉起）
uvicorn[standard]>=0.27.0

# 数据校验（FastAPI 内置依赖，显式声明便于版本固定）
pydantic>=2.5.0

# HTTP 客户端（数据迁移脚本用：从 Cloudflare Worker 拉取 KV 数据）
httpx>=0.27.0
